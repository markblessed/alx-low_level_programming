#include "main.h"
/**
 * _islower - checks if the character is lowercase
 * @c: a character argument
 * Returns: returns 1 or 0 depending on condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
